<% if @ls_genre.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@ls_genre.errors.count, "error") %>
      prohibited this profile from being saved:
    </h2>
 
    <ul>
    <% @ls_genre.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>