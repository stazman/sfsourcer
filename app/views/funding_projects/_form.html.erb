<% if @funding_projects.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@funding_projects.errors.count, "error") %>
      prohibited this profile from being saved:
    </h2>
 
    <ul>
    <% @funding_projects.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>